# Optional API base URL for hosted builds or custom local targets.
# Leave blank in Vite dev to use the /api proxy below.
# VITE_API_URL=https://api.example.com/api

# Local Express URL for Vite dev proxy (default http://localhost:5000)
# VITE_PROXY_TARGET=http://localhost:5000
